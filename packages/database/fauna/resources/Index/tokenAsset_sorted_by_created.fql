/**
 * Index for iterating over TokenAssets by creation.
 */
CreateIndex({
  name: 'tokenAsset_sorted_by_created',
  source: Collection('TokenAsset'),
  values: [{ field: ['data', 'created'] }, { field: 'ref' }],
})
